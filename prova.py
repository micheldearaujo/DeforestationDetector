import pandas as pd

# A biblioteca pandas é bastante popular. A biblioteca permite diversos atalhos para utilizar indiretamente as bibliotecas numpy e matplot.
#
# A biblioteca pandas oferece a função ____ que realizada a carga eficiente de dados em memória principal usando numpy. Em seguida, gráficos podem ser utilizados com a biblioteca ____ que oferece uma matriz quadrada formada por gráficos de barra e de distribuição usando matplot. Com uma biblioteca, o cientista de dados encontra a conveniência de diversas funções de uso frequente, por exemplo, a função ____ que calcula desvio padrão.
#
# Para completar este texto, as palavras são respectivamente:
#
# 1. read_csv(), barplot(), average()
#
# 2. read_csv(), scatter_matrix, std()
#
# 3. input(), matplot(), average()
#
# 4. input(), pyplot(), std()
#
# Quais destas alternativas é a opção CORRETA?
#
# Required answer
# read_csv(), scatter_matrix, std()
# read_csv(), barplot(), average()
# input(), matplot(), average()
# input(), pyplot(), std()


# Considere o programa incompleto a seguir:
#
# for i in ___ :
# print(i)
#
# Assinale a alternativa que completa o programa de forma que a execução deste apresente os números pares entre 0 e 20, incluindo os dois extremos.
#
# 1. range(0, 21, 1)
#
# 2. range(0, 20, 1)
#
# 3. range(0, 21, 2)
#
# 4. range(0, 20, 2)
